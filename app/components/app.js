'use strict';

const React = require('react');

const App = React.createClass({

    render() {

        return (
            <div>
                <h1>Hello, world!</h1>
            </div>
        );
    }
});

module.exports = App;
